<h1>List of Directors</h1>

<a href="http://localhost:3000/directors/new">Add a New Director</a>

<table>
  <tr>
    <th>Photo</th>
    <th>Name</th>
    <th>Bio</th>
    <th>DoB</th>
    <th colspan="3">Actions</th>
  </tr>
  <% @list_of_directors.each do |director| %>
  <tr>
    <td>
      <img src="<%= director.image_url %>" width="80">
    </td>
    <td>
      <%= director.name %>
    </td>
    <td>
      <%= director.bio %>
    </td>
    <td>
      <%= director.dob %>
    </td>
    <td>
      <a href="http://localhost:3000/directors/<%= director.id %>/show">Show</a>
    </td>
    <td>
      <a href="http://localhost:3000/directors/<%= director.id %>/edit">Edit</a>
    </td>
    <td>
      <a href="http://localhost:3000/directors/delete_director/<%= director.id %>">Delete</a>
    </td>
  </tr>
  <% end %>
</table>
